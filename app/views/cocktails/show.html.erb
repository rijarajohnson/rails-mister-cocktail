<h1><%= @cocktail.name %></h1>
<ul>
  <% @cocktail.doses.each do |dose| %>
 <li><%= dose.description %> <%= link_to "X", dose_path(dose), method: :delete %></li>
 <% end %>
</ul>
<p><%= link_to "Back to Cocktails", cocktails_path %></p>
<p><%= link_to "Add a dose", new_cocktail_dose_path(@cocktail) %></p>
